# Local: optional; Vite dev proxies /api to localhost:3001 without this.
# Netlify: add VITE_API_URL in Site settings → Environment variables (build-time).
# Use your deployed API origin, no trailing slash, e.g.:
# VITE_API_URL=https://tract-api.up.railway.app
